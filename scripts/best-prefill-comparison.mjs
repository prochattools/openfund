#!/usr/bin/env node
/**
 * DEPRECATED: This script has been replaced with the TypeScript CLI.
 *
 * To run the best-prefill comparison:
 *   npm run build:server
 *   node dist/server/cli/runBestPrefillComparison.js [--json]
 *
 * Or via npm script (once added to package.json):
 *   npm run best-prefill-comparison
 */
console.error('This script is deprecated. Use the TypeScript CLI instead:');
console.error('  npm run build:server');
console.error('  node dist/server/cli/runBestPrefillComparison.js');
process.exit(1);
